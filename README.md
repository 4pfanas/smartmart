# SmartMart

A front-end supermarket storefront for Dubai: product catalog with filters and search, categories, a local cart, contact page and order tracking. Cart data lives in the browser only. Note: some nav links point to `login.php`, `register.php` and `index.php`, which are not part of this repo yet.

## Run it

No build step. Open `index.html` in a browser.
